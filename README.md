# Secure-Login-System
Here is a complete, self-contained Python web application implementing a secure authentication system using Flask, Argon2 (via passlib or cryptography/argon2-cffi), SQLite (parameterized queries to eliminate SQL injection), secure server-side session management, input validation, and Time-Based One-Time Password (TOTP / 2FA) compatible
